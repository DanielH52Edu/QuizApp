import supabase from "./Supabase";
import {Text} from "react-native";
import {getResult} from "./QuizData";

// get result data from the ResultScreen.tsx?
// show parsedQuiz.title, result.title, result.description, score

export async function submitResult (result) {
    //grab data using getResult from QuizData????
    //then save it to the supabase?
    // then comfirmation alert?
}